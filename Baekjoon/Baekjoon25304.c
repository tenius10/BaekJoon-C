/*#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int amount; scanf("%d", &amount);
	int n; scanf("%d", &n);
	int sum=0;
	int cost, count;
	for (int i = 0; i < n; i++) {
		scanf("%d %d", &cost, &count);
		sum += cost * count;
	}
	if (amount == sum) printf("Yes");
	else printf("No");
	return 0;
}*/